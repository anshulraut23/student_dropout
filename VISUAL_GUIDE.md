# 📸 Visual Guide - Fix Score Entry Error

## What You're Seeing

```
❌ Error in Browser Console:
   - TypeError: not found
   - Failed to fetch
   - Endpoint not found
   - Score submission error
```

## Why This Happens

```
Frontend (React)          Backend (Node.js)
Port 5173                 Port 5000
   |                         |
   |  API Call: POST         |
   |  /api/marks/bulk        |
   |------------------------>|
   |                         |
   |  ❌ Connection Refused  |
   |  (Backend not running)  |
   |<------------------------|
   |                         |
   ❌ Error!                 ❌ Not Running!
```

## The Solution

```
Step 1: Start Backend
┌─────────────────────────────────┐
│ Terminal 1                      │
│                                 │
│ $ cd backend                    │
│ $ npm start                     │
│                                 │
│ ✅ Server running on port 5000 │
│ ✅ Database connected           │
└─────────────────────────────────┘

Step 2: Backend is Now Running
┌─────────────────────────────────┐
│ Backend Server                  │
│ http://localhost:5000           │
│                                 │
│ ✅ /api/health                  │
│ ✅ /api/marks                   │
│ ✅ /api/marks/bulk              │
│ ✅ All endpoints ready          │
└─────────────────────────────────┘

Step 3: Frontend Can Connect
┌─────────────────────────────────┐
│ Frontend (React)                │
│ http://localhost:5173           │
│                                 │
│ ✅ Can call API                 │
│ ✅ Can save marks               │
│ ✅ No errors                    │
└─────────────────────────────────┘
```

## How to Start Backend

### Option 1: Quick Script (Easiest)

```
Windows:
┌─────────────────────────────────┐
│ 1. Find file:                   │
│    check-and-start-backend.bat  │
│                                 │
│ 2. Double-click it              │
│                                 │
│ 3. Wait for:                    │
│    "Server running on port 5000"│
└─────────────────────────────────┘

Mac/Linux:
┌─────────────────────────────────┐
│ $ chmod +x check-and-start-     │
│   backend.sh                    │
│ $ ./check-and-start-backend.sh  │
│                                 │
│ Wait for:                       │
│ "Server running on port 5000"   │
└─────────────────────────────────┘
```

### Option 2: Manual Start

```
┌─────────────────────────────────┐
│ Step 1: Open Terminal           │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ Step 2: Navigate to backend     │
│ $ cd backend                    │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ Step 3: Install (first time)    │
│ $ npm install                   │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ Step 4: Start server            │
│ $ npm start                     │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ Step 5: See success message     │
│ ✅ Server running on port 5000  │
│ ✅ Database connected            │
└─────────────────────────────────┘
         ↓
┌─────────────────────────────────┐
│ Step 6: Keep terminal open!     │
│ (Don't close this window)       │
└─────────────────────────────────┘
```

## Verify It's Working

### Test 1: Health Check

```
┌─────────────────────────────────┐
│ Open Browser                    │
│                                 │
│ Go to:                          │
│ http://localhost:5000/api/health│
│                                 │
│ Should see:                     │
│ {                               │
│   "status": "ok",               │
│   "message": "Server is running"│
│ }                               │
└─────────────────────────────────┘
```

### Test 2: Marks Endpoint

```
┌─────────────────────────────────┐
│ Open Browser                    │
│                                 │
│ Go to:                          │
│ http://localhost:5000/api/marks │
│                                 │
│ Should see:                     │
│ - JSON response, OR             │
│ - 401 Unauthorized (OK!)        │
│                                 │
│ Should NOT see:                 │
│ - "Cannot GET /api/marks"       │
│ - 404 Not Found                 │
└─────────────────────────────────┘
```

## Now Test Score Entry

```
Step 1: Login
┌─────────────────────────────────┐
│ http://localhost:5173           │
│                                 │
│ Login as Teacher                │
└─────────────────────────────────┘
         ↓
Step 2: Go to Dashboard
┌─────────────────────────────────┐
│ Click "Dashboard" in menu       │
│                                 │
│ See list of exams               │
└─────────────────────────────────┘
         ↓
Step 3: Enter Marks
┌─────────────────────────────────┐
│ Click "Enter Marks" on an exam  │
│                                 │
│ Enter marks for students        │
│                                 │
│ Click "Save All Marks"          │
└─────────────────────────────────┘
         ↓
Step 4: Success!
┌─────────────────────────────────┐
│ ✅ "Marks saved successfully!"  │
│ ✅ "Entered: X, Failed: 0"      │
│                                 │
│ No errors in console            │
└─────────────────────────────────┘
```

## What You Should See

### Backend Terminal (Keep Open)

```
┌─────────────────────────────────────────┐
│ > npm start                             │
│                                         │
│ > backend@1.0.0 start                   │
│ > node server.js                        │
│                                         │
│ Server running on port 5000             │
│ API available at http://localhost:5000  │
│ Database connected successfully         │
│                                         │
│ POST /api/marks/bulk 201 45.123 ms      │
│ ✅ Marks saved successfully             │
│                                         │
│ (Keep this window open!)                │
└─────────────────────────────────────────┘
```

### Browser Console (F12)

```
┌─────────────────────────────────────────┐
│ Console                                 │
│                                         │
│ Submitting marks: {examId: "...", ...} │
│ Response: {success: true, entered: 5}   │
│ ✅ No errors                            │
│                                         │
│ (Should be clean, no red errors)        │
└─────────────────────────────────────────┘
```

### Browser Network Tab (F12 → Network)

```
┌─────────────────────────────────────────┐
│ Network                                 │
│                                         │
│ Name          Status  Type    Size      │
│ marks/bulk    201     xhr     1.2 KB    │
│                                         │
│ Request URL:                            │
│ http://localhost:5000/api/marks/bulk    │
│                                         │
│ Response:                               │
│ {success: true, entered: 5, failed: 0}  │
└─────────────────────────────────────────┘
```

## Troubleshooting Visual Guide

### Problem: Port Already in Use

```
❌ Error: EADDRINUSE: address already in use :::5000

Solution:
┌─────────────────────────────────┐
│ Windows:                        │
│ $ netstat -ano | findstr :5000  │
│ $ taskkill /PID <PID> /F        │
│                                 │
│ Mac/Linux:                      │
│ $ lsof -i :5000                 │
│ $ kill -9 <PID>                 │
└─────────────────────────────────┘
```

### Problem: Module Not Found

```
❌ Error: Cannot find module 'express'

Solution:
┌─────────────────────────────────┐
│ $ cd backend                    │
│ $ rm -rf node_modules           │
│ $ npm install                   │
│ $ npm start                     │
└─────────────────────────────────┘
```

### Problem: Database Error

```
❌ Error: SQLITE_ERROR: no such table: marks

Solution:
┌─────────────────────────────────┐
│ $ cd backend/storage            │
│ $ rm education_assistant.db     │
│ $ cd ..                         │
│ $ npm start                     │
│                                 │
│ (Database will be recreated)    │
└─────────────────────────────────┘
```

## Quick Reference

### Ports

```
┌─────────────────────────────────┐
│ Backend:  http://localhost:5000 │
│ Frontend: http://localhost:5173 │
└─────────────────────────────────┘
```

### Important URLs

```
┌─────────────────────────────────────────┐
│ Health Check:                           │
│ http://localhost:5000/api/health        │
│                                         │
│ Marks Endpoint:                         │
│ http://localhost:5000/api/marks         │
│                                         │
│ App:                                    │
│ http://localhost:5173                   │
└─────────────────────────────────────────┘
```

### Commands

```
┌─────────────────────────────────┐
│ Start Backend:                  │
│ $ cd backend && npm start       │
│                                 │
│ Start Frontend:                 │
│ $ cd proactive-education-       │
│   assistant && npm run dev      │
│                                 │
│ Test Backend:                   │
│ $ node test-backend.js          │
└─────────────────────────────────┘
```

## Summary

```
┌─────────────────────────────────────────┐
│                                         │
│  The Problem:                           │
│  ❌ Backend server not running          │
│                                         │
│  The Solution:                          │
│  ✅ Start backend server                │
│                                         │
│  How:                                   │
│  1. cd backend                          │
│  2. npm start                           │
│  3. Keep terminal open                  │
│                                         │
│  Result:                                │
│  ✅ Score entry works!                  │
│  ✅ All features work!                  │
│  ✅ No more errors!                     │
│                                         │
└─────────────────────────────────────────┘
```

---

**That's it! Start the backend and everything will work! 🎉**
